#JSGF V1.0;

grammar vpc_service;


public <greet> = hello;
public <run> = run <app>;
public <global> = show all | close | hide all | switch right | switch left |
                  scroll down | scroll up | cancel | search;

       <app> = browser | mail | music | notes;